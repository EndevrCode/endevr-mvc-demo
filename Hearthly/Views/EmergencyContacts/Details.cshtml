@using Hearthly.Data
@model EmergencyContact

@{
    ViewData["Title"] = "Contact Details";
}

<div class="mb-4">
    <h2 class="mb-0"><i class="fa-light fa-address-card me-2"></i>Emergency Contact</h2>
</div>

<div class="glass-card p-4 mb-4">
    <div class="row gy-3 mb-4">
        <div class="col-6">
            <div class="text-muted small">Type</div>
            <div>@Model.ContactType</div>
        </div>
        <div class="col-6">
            <div class="text-muted small">Name</div>
            <div class="fw-semibold">@Model.Name</div>
        </div>
        <div class="col-12">
            <div class="text-muted small">Phone</div>
            <div>
                <a href="tel:@Model.PhoneNumber" class="text-decoration-none">
                    <i class="fa-light fa-phone me-1 text-muted"></i>@Model.PhoneNumber
                </a>
            </div>
        </div>
        @if (!string.IsNullOrWhiteSpace(Model.Notes))
        {
            <div class="col-12">
                <div class="text-muted small">Notes</div>
                <div>@Model.Notes</div>
            </div>
        }
    </div>

    <div class="d-flex justify-content-center flex-wrap gap-2">
        <a href="tel:@Model.PhoneNumber" class="btn btn-danger rounded-pill px-4">
            <i class="fa-light fa-phone-arrow-up-right me-1"></i> Call Now
        </a>
        <a asp-action="Edit" asp-route-id="@Model.Id" asp-route-familyId="@Model.FamilyId"
           class="btn btn-outline-primary rounded-pill px-4">
            <i class="fa-light fa-pen-to-square me-1"></i> Edit
        </a>
        <button type="button" class="btn btn-outline-danger rounded-pill px-4"
                data-name="@Model.Name"
                onclick="confirmDelete(this)">
            <i class="fa-light fa-trash me-1"></i> Delete
        </button>
        <a asp-action="Index" asp-route-familyId="@Model.FamilyId"
           class="btn btn-outline-secondary rounded-pill px-4">
            <i class="fa-light fa-arrow-left me-1"></i> Back
        </a>
    </div>

    <form id="delete-form"
          asp-action="Delete" asp-route-id="@Model.Id" asp-route-familyId="@Model.FamilyId"
          method="post" style="display:none;">
        @Html.AntiForgeryToken()
    </form>
</div>

@section Scripts {
<script>
function confirmDelete(btn) {
    var name = btn.dataset.name;
    Swal.fire({
        titleText: 'Delete ' + name + '?',
        text: 'This emergency contact will be permanently removed.',
        icon: 'warning',
        showCancelButton: true,
        confirmButtonText: 'Delete',
        confirmButtonColor: '#dc3545',
        cancelButtonText: 'Cancel'
    }).then(function (r) {
        if (r.isConfirmed) document.getElementById('delete-form').submit();
    });
}
</script>
}
